manejadores de código: yarn, npm
Instalar librerías:
    npm i bootstrap --save // dependencia de proyecto 
    -D dependencia de desarrollo. (--save-dev)

instalar dependencias de forma personalizada: npm i xxx --prod

https://github.com/cursosdevfull/ProyectosAngular-01

sólo se puede interpolar propiedades o métodos

INTERPOLACIÓN (son las {{}}): si la propiedad no existe, Angular la crea.
null se asigna, undefined no.

Angular v9 para adelante en navegadores 

NgModel trabaja con FormsModule

TypeScript no soporta herencia múltiple ni sobrecarga de métodos.

[formControl]="grupo.get('correo')"

directivas estructurales:
    agregan o quitan elementos del DOM
    window, navigator
    tienen prefijo * 
    ngIf, ngFor, ngSwicht

directivas atributos:
    modificar aspecto
    agregar funcionalidad.
    ngStyle, ngClass

